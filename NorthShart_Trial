
    local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
    local Window = Library.CreateLib("TEST", "DarkTheme")

    -- MAIN

	local Main = Window:NewTab("Ellesmere Functions")
    local MainSection = Main:NewSection("Reset Before Teleporting")
MainSection:NewToggle("InstantClaim", "Toggle instant claiming", function(state)
    if state then
        _G.InstantClaim = true

        task.spawn(function()
            while _G.InstantClaim do
                for _, player in ipairs(game:GetService("Players"):GetPlayers()) do
                    if not _G.InstantClaim then
                        break
                    end

                    game:GetService("ReplicatedStorage").DefinEvents.InstanceRequestFunction:InvokeServer(player, "CompleteBounty")
                end

                task.wait()
            end
        end)

    else
        _G.InstantClaim = false
    end
end)

MainSection:NewButton("Set Platform", "Teleport", function()
local part = Instance.new("Part")
part.Size = Vector3.new(20, 3, 25)
part.CFrame = CFrame.new(1884,96,-2400)
part.Anchored = true
part.Parent = workspace
end)

MainSection:NewButton("TP Outside Jail - Start", "Teleport", function()
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character
Character.HumanoidRootPart.CFrame = CFrame.new(1884,101,-2400)
end)


MainSection:NewToggle("TP Outside Jail - Farm", "Toggles automatic teleport and reset", function(state)
    if state then
        _G.AutoTP = true

        task.spawn(function()
            while _G.AutoTP do
                local Players = game:GetService("Players")
                local ReplicatedStorage = game:GetService("ReplicatedStorage")
                local Character = Players.LocalPlayer.Character
                local HRP = Character and Character:FindFirstChild("HumanoidRootPart")

                if HRP then
                    local targetPosition = Vector3.new(1884, 101, -2400)

                    HRP.CFrame = CFrame.new(targetPosition)

                    task.wait(2)

                    if not _G.AutoTP then
                        break
                    end

                    if (HRP.Position - targetPosition).Magnitude <= 10 then
                        local args = {
                            [1] = ReplicatedStorage.Interacting,
                            [2] = "Reset"
                        }

                        ReplicatedStorage.DefinEvents.InstanceRequestFunction:InvokeServer(unpack(args))
                    end
                end

                task.wait(1)
            end
        end)

    else
        _G.AutoTP = false
    end
end)

MainSection:NewButton("Board TP", "Teleport", function()
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character
Character.HumanoidRootPart.CFrame = CFrame.new(-126,56,-2581)
end)

wait(0.1)
 loadstring(game:HttpGet("https://raw.githubusercontent.com/hassanxzayn-lua/Anti-afk/main/antiafkbyhassanxzyn"))();
 wait(0.1)
